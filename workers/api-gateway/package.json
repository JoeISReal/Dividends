{
    "name": "dividends-api-gateway",
    "version": "1.0.0",
    "description": "Cloudflare Worker API Gateway for DIVIDENDS",
    "main": "src/worker.js",
    "scripts": {
        "deploy": "wrangler deploy",
        "dev": "wrangler dev",
        "tail": "wrangler tail"
    },
    "devDependencies": {
        "wrangler": "^3.0.0"
    }
}